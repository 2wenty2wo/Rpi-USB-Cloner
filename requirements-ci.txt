# CI/Test Dependencies (non-Raspberry Pi)
# Install with: pip install -r requirements-ci.txt

# Runtime dependencies needed for tests that run on Ubuntu CI
Pillow==10.1.0
aiohttp==3.9.5

# Testing framework and plugins
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
pytest-timeout>=2.2.0

# Coverage reporting
coverage[toml]>=7.3.0
