# Rpi-USB-Cloner Dependencies
# Python 3.8+ required

# Image processing library
Pillow==10.1.0

# OLED display drivers for Adafruit 128x64 OLED Bonnet
# luma.oled depends on luma.core, but we pin both for reproducibility
luma.core==2.4.2
luma.oled==3.12.0

# Raspberry Pi GPIO control
RPi.GPIO==0.7.1

# Web server (HTTP + WebSocket)
aiohttp==3.9.5

# System monitoring (CPU, memory, disk, etc.)
psutil==5.9.8
