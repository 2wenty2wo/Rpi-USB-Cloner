# Rpi-USB-Cloner Dependencies
# Python 3.8+ required

# Image processing library
Pillow==10.1.0

# OLED display drivers for Adafruit 128x64 OLED Bonnet
# luma.oled depends on luma.core, but we pin both for reproducibility
# Only install on Raspberry Pi platforms.
luma.core==2.4.2; platform_machine == "armv7l" or platform_machine == "aarch64"
luma.oled==3.12.0; platform_machine == "armv7l" or platform_machine == "aarch64"

# Raspberry Pi GPIO control (Raspberry Pi only)
RPi.GPIO==0.7.1; platform_machine == "armv7l" or platform_machine == "aarch64"

# Web server (HTTP + WebSocket)
aiohttp==3.9.5

# System monitoring (CPU, memory, disk, etc.)
psutil==5.9.8

# Structured logging
loguru==0.7.2

# Typing helpers for runtime (Python 3.8)
typing_extensions==4.12.2
